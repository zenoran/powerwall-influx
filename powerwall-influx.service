# This file is intentionally left blank.
#
# The install-service.sh script prompts for your environment details, renders
# powerwall-influx.service.template, and writes the result to
# powerwall-influx.service.local (which is gitignored). The generated unit file
# is then copied into /etc/systemd/system during installation.
